class StorageService {
  static Future<void> init() async {

    
    // Future app-level initializations
    // Example:
    // SharedPreferences
    // Secure Storage
    // Local DB
    // Theme settings

    await Future.delayed(
      const Duration(milliseconds: 300),
    );
  }
}